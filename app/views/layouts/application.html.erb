<!DOCTYPE html>
<html ng-app>
<head>
  <title>SC4G</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <!--<link href="/assets/bootstrap.css" rel="stylesheet">-->
  <!--<link href="/assets/bootstrap-responsive.css" rel="stylesheet">-->
  <!--<script src="/assets/bootstrap.js" type="text/javascript"></script>-->

  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <script>
      function toggleElement(elementId){
          var element=document.getElementById(elementId);
          if(!element)return true;
          if(element.style.display=="none"){
              element.style.display="block"
          }
          else{
              element.style.display="none"
          }
          return true;
      }

  </script>

</head>
<body>

<div id="user_nav" class="navbar-fixed-top">
  <p>
  <% if member_signed_in? %>
    Signed in as <%= current_member.email %>. Not you?
    <%= link_to "Sign out", destroy_member_session_path, :method => :delete %>
  <% else %>
    <%= link_to "Sign up", new_member_registration_path %> or <%= link_to "sign in", new_member_session_path %>
  <% end %>
  </p>
</div>

<%= yield %>
<footer class="navbar-fixed-bottom">
<%= debug(params) if Rails.env.development? %></footer>

</body>
</html>
